# .streamlit/config.toml

[theme]
primaryColor = "#83c9ff"  # Steel Blue for accents
backgroundColor = "#daefff"  # Dark background similar to VS Code
secondaryBackgroundColor = "#c1e4ff"  # Slightly lighter dark shade for secondary backgrounds
textColor = "#3d404b"  # White text for readability
font = "sans serif"

[server]
# Your server configuration here (if needed)

[browser]
# Any browser-specific configurations

# Attempt to minimize the menu (Note: This does not disable the auto-generated sidebar for pages)
[deprecation]
showMenu = false  # This attempts to hide the hamburger menu, not the sidebar for pages